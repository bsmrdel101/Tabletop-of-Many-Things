export const alert = (msg: string) => window.alert(msg);

export const confirm = (msg: string) => window.confirm(msg);

export const prompt = (msg: string, defaultValue?: string) => window.prompt(msg, defaultValue);
