export type Task = {
    name: string
    desc: string
};

export type Game = {
    id: number,
    name: string,
    code: string
};

export type Client = {
    id: number,
    clientType: string
};
